class User {
  String displayName;
  String email;
  String uid;
  String password;
  bool returnSecureToken;

  User(this.displayName, this.email, this.uid, this.password,
      this.returnSecureToken);
}
